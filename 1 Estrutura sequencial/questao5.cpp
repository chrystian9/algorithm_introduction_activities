#include <iostream>
using namespace std;

int main(){
	float baseMaior, baseMenor, trapAltura;
	
	cin >> baseMaior;
	cin >> baseMenor;
	cin >> trapAltura;
	cout <<  ((baseMaior + baseMenor) * trapAltura) / 2;
	return 0;
}
